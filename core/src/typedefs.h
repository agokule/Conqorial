#ifndef PLAYERID_H
#define PLAYERID_H

#include <cstdint>

// PlayerId is an alias for uint8_t.
// If it is 0, it means no player
typedef uint8_t PlayerId;

// should be between 0 and 1
typedef double Elevation;

#endif // PLAYERID_H
